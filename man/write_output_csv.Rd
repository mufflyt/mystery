% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_output_csv.R
\name{write_output_csv}
\alias{write_output_csv}
\title{Write a Data Frame to CSV with Robust Logging and Error Handling}
\usage{
write_output_csv(
  df,
  filename,
  output_dir = "ortho_sports_med/Figures",
  verbose = TRUE
)
}
\arguments{
\item{df}{A data frame to be written to a CSV file.}

\item{filename}{A string specifying the name of the output file (with .csv extension).}

\item{output_dir}{A string specifying the directory where the CSV file will be saved. Default is \code{"ortho_sports_med/Figures"}.}

\item{verbose}{A boolean indicating whether to print detailed logs. Default is \code{TRUE}.}
}
\value{
NULL. The function saves the CSV file to the specified location.
}
\description{
This function writes a data frame to a CSV file in the specified output directory.
It includes detailed logging to inform the user of the function's progress and any potential issues.
}
\details{
\subsection{Key Features of the Function:}{
\itemize{
\item \strong{Directory Creation}: If the specified output directory does not exist, the function attempts to create it.
\item \strong{Logging}: Verbose logging informs the user of the progress and any potential errors during the writing process.
\item \strong{Error Handling}: The function handles errors such as invalid input types or directory creation failures.
}
}
}
\examples{
# Example 1: Save a data frame to the default directory with detailed logging
df <- data.frame(Name = c("John", "Jane"), Age = c(30, 25))
write_output_csv(df, "output.csv")

# Example 2: Save a data frame to a custom directory without logging
df <- data.frame(Product = c("Apple", "Banana"), Price = c(1.2, 0.5))
write_output_csv(df, "output.csv", output_dir = "custom/directory", verbose = FALSE)

# Example 3: Save a large data frame with detailed logging
df_large <- data.frame(ID = 1:1000, Value = rnorm(1000))
write_output_csv(df_large, "large_output.csv", output_dir = "data/outputs", verbose = TRUE)

}
