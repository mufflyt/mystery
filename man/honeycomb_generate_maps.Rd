% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_physicians_honeycomb.R
\name{honeycomb_generate_maps}
\alias{honeycomb_generate_maps}
\title{Generate Hexagon Maps by ACOG District with Logging and Error Handling}
\usage{
honeycomb_generate_maps(
  physician_locations_sf = NULL,
  acog_districts_sf = NULL,
  trait_map = "all",
  honeycomb_map = "all",
  hex_grid_size = c(0.3, 0.3),
  target_district = NULL
)
}
\arguments{
\item{physician_locations_sf}{A Simple Features (sf) object containing physician data with coordinates.
If NULL, default sample data is used.}

\item{acog_districts_sf}{A Simple Features (sf) object containing the grouped ACOG districts.
If NULL, default ACOG districts are used.}

\item{trait_map}{A string specifying the trait map (default is "all").}

\item{honeycomb_map}{A string specifying the honey map (default is "all").}

\item{hex_grid_size}{A numeric vector of length 2 specifying the grid size for the hexagon map
(default is c(0.3, 0.3)).}

\item{target_district}{A string or NULL to specify a specific district for generating the map
(default is NULL, which processes all districts).}
}
\value{
A list of ggplot objects of the generated maps for the specified or all districts.
Each ggplot object is a hexagon map showing physician distribution.
}
\description{
This function generates hexagon maps for ACOG districts, using physician location data.
It provides extensive logging, error handling, and defaults to sample data if no inputs are provided.
}
