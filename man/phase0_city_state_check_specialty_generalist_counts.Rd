% Generated by roxygen2: do not edit by hand
% Please edit documentation in
%   R/phase0_city_state_check_specialty_generalist_counts.R
\name{phase0_city_state_check_specialty_generalist_counts}
\alias{phase0_city_state_check_specialty_generalist_counts}
\title{Check City-State Combinations for Minimum Generalists and Specialists with Logging and Summary}
\usage{
phase0_city_state_check_specialty_generalist_counts(
  data,
  min_generalists,
  min_specialists,
  generalist_name = "General Dermatology",
  specialist_name = "Pediatric Dermatology",
  failing_csv_path = NULL,
  successful_csv_path = NULL
)
}
\arguments{
\item{data}{A data frame containing professional information. Must include at least the columns \code{city}, \code{state_code}, and \code{specialty_primary}.}

\item{min_generalists}{An integer specifying the minimum number of generalists required per city-state combination.}

\item{min_specialists}{An integer specifying the minimum number of specialists required per city-state combination.}

\item{generalist_name}{A string specifying the specialty name for generalists. Default is \code{"General Dermatology"}.}

\item{specialist_name}{A string specifying the specialty name for specialists. Default is \code{"Pediatric Dermatology"}.}

\item{failing_csv_path}{An optional string specifying the file path to save the failing combinations CSV. Default is \code{NULL} (no file saved).}

\item{successful_csv_path}{An optional string specifying the file path to save the successful combinations CSV. Default is \code{NULL} (no file saved).}
}
\value{
A list containing two data frames: \code{failing_combinations} and \code{successful_combinations}.
}
\description{
This function checks if each city-state combination has the required number of generalists and specialists.
It logs inputs, transformations, outputs, and returns two data frames: one with the failing city-state-specialty combinations
and one with the successful combinations. Optionally, the results can be saved as CSV files.
}
\details{
\subsection{Key Features:}{
\itemize{
\item \strong{Generalists vs. Specialists}: You can specify the names of generalists and specialists, and the function checks if
each city-state combination has the required number of each.
\item \strong{Logging}: Extensive logging ensures that inputs, transformations, and results are tracked.
\item \strong{CSV Output}: Optionally, the function writes both failing and successful city-state-specialty combinations to separate CSV files.
\item \strong{Summary Logging}: A summary of the \code{min_generalists}, \code{min_specialists}, and the results is logged at the end.
}
}
}
