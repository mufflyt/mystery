% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figure_create_bar_plot.R
\name{create_bar_plot}
\alias{create_bar_plot}
\title{Create a Bar Plot with Total Sample Size in the Title}
\usage{
create_bar_plot(
  input_data,
  category_var,
  grouping_var,
  title = NULL,
  x_axis_label = NULL,
  y_axis_label = "Count",
  output_directory = "output",
  filename_prefix = "bar_plot",
  verbose = TRUE
)
}
\arguments{
\item{input_data}{A dataframe containing the data to be plotted.}

\item{category_var}{A string representing the column name for the categorical variable on the x-axis (e.g., insurance type).}

\item{grouping_var}{A string representing the column name for the facet wrap (grouping variable).}

\item{title}{A string specifying the title of the plot. Default is \code{NULL}, and the function will generate a title based on \code{category_var} and \code{grouping_var}.}

\item{x_axis_label}{A string specifying the label for the x-axis. Default is \code{NULL}, and the function will use the column name for the x-axis variable.}

\item{y_axis_label}{A string specifying the label for the y-axis. Default is "Count".}

\item{output_directory}{A string representing the directory where the plot files will be saved. Default is "output".}

\item{filename_prefix}{A string used as the prefix for the generated plot filenames. Default is "bar_plot".}

\item{verbose}{A boolean indicating whether to print messages about the saved plot locations. Default is TRUE.}
}
\value{
This function returns the plot object for further manipulation or saving.
}
\description{
This function generates a bar plot based on a categorical variable and facets the plot by a grouping variable.
The plot title automatically includes the total sample size (N). The function also supports custom axis labels,
and it returns the plot object for further manipulation or saving.
}
\examples{
# Example 1: Basic usage with a categorical and facet variable
create_bar_plot(
  input_data = my_data,
  category_var = "insurance_type",
  grouping_var = "region",
  title = "Insurance Type Distribution by Region",
  x_axis_label = "Insurance Type",
  y_axis_label = "Number of Observations"
)
}
